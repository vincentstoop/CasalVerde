<h1>Pages</h1>

<%= link_to "Edit carousel pictures", admin_page_photos_path(@homepage.id) %>

<% @pages.each do |page| %>
  <h2><%= page.name.gsub(/[^0-9A-Za-z]/, '').capitalize %></h2>
  <% if page.name == "home_page" %>
    <%= link_to "Edit paragraphs", admin_page_paragraphs_path(page.id) %>
  <% end %>
    <!-- Link to paragraph edit -->
<% end %>
