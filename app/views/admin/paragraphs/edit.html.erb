<h1>Edit paragraph <%= @paragraph.id %></h1>
<%= form_for ['admin', @page, @paragraph] do |f| %>
  <div class="form-group">
    <%= f.label :text %>
    <%= f.text_area :text %>
  </div>
  <div class="form-group">
    <%= f.submit "Edit", id: "edit-paragraphs" %>
  </div>
<% end %>
